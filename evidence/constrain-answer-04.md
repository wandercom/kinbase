The blinding broker, not the Validator, owns the mapping. It randomizes order,
strips arm metadata, and encrypts opaque-ID-to-arm mapping to a founder-held key.
Scorers see opaque candidates and common observations. No arm aggregates stream.
Unblinding is the first successful mapping decryption, admitted only after candidate,
test, and dual-score digests are sealed in the append-only run ledger. A harness
defect must be ledgered before that event and founder-countersigned; sequence is
verifiable. The one superseding run uses the same pool and predeclared reserve seeds.

Company-steward exception authority cannot waive proof gates or verdicts. Agy may
block before evidence seal/unblinding for adherence, causing INVALID_HARNESS; it
cannot change evidence or a deterministic result. After sealing, computation is
unblockable and later objections are retained findings. Unresolved validity is
INVALID_RUN. The ratified source > Product > Architecture > Verification > generated
manifest chain is authoritative; mismatches block rather than invite adjudication.

The AuthorityRegistry stores stable authority ID, scope, public key, and opaque
channel reference. Real name/contact endpoint are separate directory personal data
with retention and never enter Codebase/proof packets. Private signing keys and
tokens are file-descriptor/keychain references, not registry bytes. Test authorities
are fictional; live founder use is opt-in. Company retains signed answer content as
a business decision under its policy; if policy requires removal, a signed tombstone
withdraws it and reopens every dependent Unknown. Removing directory/contact data
does not silently rewrite the decision.
