FACTORY_ACTIVITY cursor=2 class=pre-ratification-review-result

Simulacrum and seven-persona Advocate review completed. Do not accept the initial
specification yet. Blocking findings that survive Validator triage:

1. The `greenfield` arm is actually a fully-informed oracle-spec arm. Rename it,
   exclude accepted patch/hidden-test leakage, bind its exact fact set and token
   budget, and add an inconclusive-ceiling result when the comparator cannot reach
   target quality.
2. Headroom selection reuses a noisy baseline outcome and inflates the contrast.
   Use disjoint screening seeds or non-outcome eligibility, then fresh measurement
   seeds frozen before other arms.
3. Live human authority during only the full-system arm confounds the causal result.
   V-6 retains the live round trip; benchmark arms receive the same frozen,
   pre-change authority-answer service and are stratified by question use.
4. Qualitative scoring is not blinded. Relabel candidates to opaque IDs, commit
   scores before unblinding, use two independent raters, and gate inter-rater
   agreement.
5. Controls do not isolate mechanism. Add raw plausibly-relevant retrieval and
   maintained-corpus scalar top-k; the latter versus full system tests set-conditional
   selection on natural redundancy.
6. P-10 aggregation/sample size/power are unspecified and thresholds can be
   arithmetically impossible. Pilot on excluded tasks, freeze power-derived seed/task
   count, use aggregate CI rules and a relative gap-closure threshold.
7. Safety must rest on deterministic taint/capability gates, not stochastic model
   recall. Live classifier variability is separately measured over multiple runs.
8. Helland gaps: Company must own semantic-digest algorithm/version; divergent
   fan-out needs an apology Unknown and named owner; revocation cursor must reopen
   downstream facts; runtime evidence needs an owner/freshness clock; blocking
   Unknowns need a person and escalation clock.
9. Red-team gaps: inline-byte approval/commit TOCTOU, trust-root/key lifecycle,
   deletion/completeness manifest, origin trust for unmerged repo bytes, canonical
   encoding/delimiter injection, same-UID threat statement/probe, service auth.
10. The accepted approval-fatigue correction has no numeric/use test. Add a hard
    prompt-rate/usability gate.

Current action: revise exact phase artifacts without deleting any P-1 through P-10
capability. Assess whether these dispositions advance the founder's ultimate goal,
identify any finding that is itself a scope-erasing distraction, and return BLOCK
until the experimental and ownership flaws are closed.
