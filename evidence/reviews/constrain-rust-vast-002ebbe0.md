# Constrain review record — Rust/Vast amendment

Status: **challenge evidence accepted selectively; synthesis rejected; not authority**

- Session: `002ebbe0-2b13-4c6b-836b-921cb2ea3339`
- Target on entry: candidate amendment
  `9b2e1f85b0d84ae6c9ccc7aeb13f410654085a77aa748b975f72998391d71c49`
- Interview: 6 understand rounds, 10 challenge rounds
- Completed-at record: `2026-09-06T02:21:44.967607Z`
- Validator: `codex-root-validator`

The session was used as a constraint elicitor and adversarial interviewer. Its
generated topology never outranks the ratified Product/Architecture or the amendment.

## Accepted challenge findings

1. A 0.90 weighted composite could pass with only 0.80 functional behavior when all
   other components were perfect. The amendment adds independent full-system
   functional quality 0.90 and functional oracle-equivalence ±0.05 co-primary gates.
2. Every new endpoint changes the power problem. No pilot/Product run has occurred;
   fresh excluded pilots and the fixed joint-power program must select a new N before
   measurement.
3. Universal abstention could game temporal safety. P-5/V-5 now require at least a
   1:1 substantive mirror set of valid, unexpired, single-authority facts and zero
   false `Unknown` results.
4. Narrative attribution was too weak when `authority-only` matched full-system. The
   amendment makes the lower paired 95% `full-system - authority-only >= 0.10` a
   co-primary conjunct whose miss is `NOT_PROVEN`.
5. Once gated, authority-only configuration became an outcome lever. A schema-blind,
   independent Oracle Curator must optimize the strongest competent one/two neutral
   questions under a frozen effort/tool/call protocol; the replies cannot contain the
   complete oracle and at least one load-bearing maintained-corpus fact must remain.
6. Missing/blocked ordinary cells remain preregistered intention-to-treat zeros and
   are never dropped/replaced. Pilot variance includes block/refusal/timeout/no-patch
   outcomes.
7. Candidate egress is hold-back, never scrub-and-grade. A confirmed recovery is a
   zero plus Product privacy failure; a confirmed scanner false positive or unresolved
   recovery dispute is `INVALID_HARNESS`, so comparator false positives cannot buy a
   lift margin.
8. Boundary admission checks authorized source/tool-result lineage and absence of
   forbidden provenance before byte scanning; scanning is defense in depth rather
   than the paraphrase boundary.
9. The Tester-owned HMAC key dies with raw canaries within 24 hours absent the already
   bounded founder+security-custodian incident hold. The permanent HMAC has no
   surviving confirmation key after erasure.
10. `.kin/events` contain only exact approved minimized Codebase facts/references plus
    authority metadata; manifests contain digests/counts/heads. Personal bodies,
    transcript paths/digests, cross-store correlation IDs, secrets, raw canaries,
    keys, and reversible mappings remain excluded.
11. Personal never governs coding. A private rejection is not a Company withdrawal;
    forbidden/out-of-scope evidence cannot define oracle correctness.

## Synthesis disposition

The first synthesis invented a universal Kindex graph engine/router and network
topology, contradicting the physical-store design. The Validator rejected it. A
Claude revision stalled twice after feedback. The Codex backend failed because
Constrain passed an unsupported `--ask-for-approval` flag to the installed Codex CLI.
The OpenAI revision again invented a cross-store `Corpora Manager`, Postgres, gRPC,
ports, trust floors/decay, and sensitivity tiers not supplied by authority. After a
bounded correction it produced invalid YAML and incomplete artifacts.

Constrain wrote five untracked root artifacts; the Validator deleted them. It also
auto-published project-graph node `c934280f9aa4`; the Validator invalidated that node
at `2026-09-06T02:22:42.079083Z` with disposition `REJECTED_SYNTHESIS`.

The accepted material is the numbered challenge record above. No generated
`prompt.md`, `constraints.yaml`, `trust_policy.yaml`, `component_map.yaml`, or
`schema_hints.yaml` is admitted.
