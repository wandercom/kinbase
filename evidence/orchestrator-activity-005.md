# Orchestrator activity delta — cursor 5

Current action: finish the last outcome/security amendments before exact-byte
ratification. No Coder or Tester implementation lane has started.

Ultimate goal is unchanged: execute a real three-product Kindex proof that ingests
and maintains heterogeneous evidence, separates Personal/Company/Codebase without
unauthorized durable disclosure, discerns authority and time, asks the real architect,
works through native Codex and Claude lifecycle, and raises brownfield coding into the
oracle-spec equivalence band. Structure, mocks, green unit tests, or an underpowered
benchmark cannot pass.

Since cursor 4 NO-OP:

- Final Simulacrum found causal-proof seams. The contract now seals historical corpus
  snapshots before task knowledge, adds a null-system scaffold control, a frozen
  generic static-prior control, and Company-only/Codebase-only contribution ablations,
  binds every arm/grader field, counts all admitted launches intention-to-treat, and
  assigns outcome-blind run-integrity review before unblinding.
- The experiment now has eleven arms. Full must beat null by 0.15, static prior and
  both scalar top-k controls by 0.10, and each single-store ablation by 0.10 on its
  predeclared source stratum, as well as reach 0.90 and the powered TOST oracle band.
  Personal is not exposed to a coding arm; P-1/P-2/P-3/P-9 prove its private capture
  and approved shared-promotion role.
- Two implementation-blind humans freeze excluded-pilot rubric gold before
  measurement. Two automated graders from different model/provider families must
  qualify against it and remain fingerprint/prompt/parser bound. Binary fact-access
  and exact-arm inference are both measured before unblinding.
- The joint power program explicitly powers source strata at their own denominators.
  The unavoidable eleven-arm call envelope is published: N=8 reserves 693 coding and
  1,386 scoring calls; N=32 1,562/3,124; N=71 2,981/5,962; N=126 4,972/9,944, before
  other gates. Underfunded remains NOT_PROVEN, never a small-N success.
- A final 3-persona Advocate pass (Helland, adversarial, red-team; review
  20260905T050912-2cd7da1d; 36 findings) was dispositioned. Incorporated high/critical
  fixes include explicit host-owned prompt-budget shards, cross-destination four-slot
  enforcement, atomic reissue/reservation, terminal orphan abandonment, separated
  extraction-vs-truth authority, scoped criticality relaxation, expired manifest
  apology, unreachable-clone residual, exact HTTP authority-scope ACL, terminal
  control neutralization, descriptor-backed classifier execution, inherited-fd denial,
  common-dir admission locking, current-cursor nonce refresh, bounded fsck incidence,
  and a corpus-growth adequacy gate.
- Corpus Builder ordering is now executable: V-1 through V-9 pass first, the
  adapter/reducer digest freezes, then a task-blind Builder seals every candidate-parent
  snapshot. Pre-task repair rebuilds the entire census; post-exposure repair kills the
  run.

Review the current exact `spec/product.md`, `spec/architecture.md`,
`spec/threat-model.md`, `spec/verification.md`, and `spec/cli.md`. Return NO-OP only if
the amended direction still advances the founder's proof and no remaining
contradiction permits scaffolding, unsafe leakage, authority substitution, task/corpus
curation, generic-context effects, single-store noncontribution, denominator escape,
or an unexecutable measurement to pass. Otherwise BLOCK with the exact requirement;
do not reduce the founder's acceptance functionality.
