# Kinbase Factory Tester remediation 002

Resume the same implementation-blind Claude Tester role against your exact clean
tests-only commit `244184e631695cc331d64667bd20cfae7f286526` (tree
`893a6ba3bbc464762d479aa13305d3761adc07b4`). A separately bound, fresh
implementation-blind Detector Reviewer has blocked this instrument on 17 findings.
Its exact report is appended to this dispatch after the final delimiter.

You still have no product/Coder access and no verdict authority. The report
contains only ratified specs and your own test package; it is authorized feedback
to the Tester role. Do not inspect any Coder, Validator, Detector-Reviewer, main
repository, run-control, global Kindex, agent-history, or prior-review path. Do
not delegate, spawn helpers, or contact another role.

## Required outcome

Repair every blocking finding in the appended report. This is not a request to
make the current self-tests green or preserve their present shape. Rewrite or
replace weak tests, fixtures, catalogs, and harness code wherever necessary so
the instrument can actually prove or falsify the ratified V-1 through V-9
obligations before product combination.

In particular, the completed artifact must:

- represent all unexecuted, deselected, skipped, empty-parameter, missing-input,
  and incomplete-control obligations as non-green `NOT_RUN`/`INVALID_HARNESS`,
  never preinitialized PASS;
- keep instrument validity and product failure as independently evidenced
  channels until the ratified composition step;
- contain a machine-readable, total per-obligation catalog of thresholds,
  positive controls, negative controls, product mutations, detector mutations,
  surfaces/vectors, expected outcomes, and fail-closed classifications;
- provide executable Validator-applied mutation planters for every catalog entry,
  require every named `must_fail` node to fail, require paired negative controls
  to remain clean, and emit a content-addressed total kill ledger;
- keep gold, expected answers, case IDs, and semantic outcome selectors outside
  every product command, environment, prompt, fixture payload, and readable
  product surface; only raw state and narrowly scheduled, independently witnessed
  timing/ordering/failure perturbations may reach the system under test;
- construct real raw native inputs and lifecycle transitions for V-1; distinct
  hidden calibration and evaluation data plus harness-computed V-2 metrics and
  seeded sagas; every V-3 threat vector/surface/control/mutation with total scan
  and reconstruction evidence; actual Git/events/manifests/scale/replay/lock
  state for V-4; signed temporal histories for V-5; a registered live Chief
  Architect and independently logged signed round trip for V-6; a raw ingested
  candidate corpus for V-7; a real live Company/reference/cache/revocation path
  for V-8; and actual approved/denied host installation, invocation, timing,
  topology, nonzero prompt/operator work, and gold-free maintenance inputs for
  V-9;
- include a concrete eligible licensed-public auxiliary-corpus pool with frozen
  source/version/rights/provider-transmission basis, candidate contents,
  generated dictionaries/correlation records/decoys, and digests for a fresh
  Reviewer to select. Do not invent a license or copy material without a verified
  rights basis; if you cannot supply this within your role, stop with one exact
  `FACTORY_QUESTION` rather than papering over it;
- provide a Reviewer-safe product-independent collection/selftest entrypoint that
  reads only ratified `spec/**` and `tests/**`, performs no network install, and
  creates no repository files; and
- make every required assertion total over typed evidence. No bare-return pass,
  permissive missing-key default, optional assertion, empty-loop success,
  refusal-as-success, `or True`, product-reported proof claim, or zero-work pass
  may remain.

Treat the appended report's exact path/line examples, counterexamples, complete
control inventory, obligation matrices, and required remediations as blocking
review feedback. Close all 17 findings, including the full control inventory—not
only the short titles. Preserve the ratified public behavior; tests may demand it
but may not redefine it.

## Lane and completion rules

- You may modify only `tests/**` and Tester-owned fixtures/catalogs beneath it.
- Do not modify product, `spec/**`, `.kin/**`, `evidence/**`, root metadata, or
  ratification receipts.
- You may run collection and product-independent instrument self-tests only. Do
  not inspect or execute product implementation and do not issue a verdict.
- Inspect your own complete diff. Commit all changes to
  `factory/tester-ac8a13d1`, leave the tree clean, and return the exact commit.

If the ratified specs or the authorized Detector findings are genuinely
contradictory and force a semantic choice, stop with exactly
`FACTORY_QUESTION: <one concrete question>`. Otherwise end with
`FACTORY_STATUS: DONE <full commit SHA>` or
`FACTORY_STATUS: BLOCKED <reason>`. A Tester status is not admission or verdict.

--- BEGIN EXACT DETECTOR REVIEW REPORT (SHA-256 9424a1fc23de1522d2c2f8172982b93340d1c686e906c0015265e9fd8ce3dd25) ---
