# Factory Coder continuation dispatch 005 — Guildhall proof

Resume the same bound GLM-5.3 Coder thread and isolated author lane. The founder
has added usage buffer after the provider quota stop. That stop was external and
authorizes continuation only; it changes no product requirement, role, proof
claim, or authority byte.

## Immutable authority and state

- Ratified baseline: `e29f3fe03595d594c0546f9b0012b58f7c45bac1`
- Ratification manifest SHA-256:
  `ac8a13d184397fef574e173b81466ff43e6b3f91f89804c7ee797cc404a622db`
- Branch: `factory/coder-ac8a13d1`
- No Coder commit exists yet; the substantial uncommitted product work under
  `guildhall/` and `pyproject.toml` is yours from the preceding turns.
- The last successful operation before quota exhaustion inspected four
  checkpoint filters in `guildhall/adapters.py`. This is only a transport
  checkpoint, not an instruction to accept the preceding reasoning. Reinspect
  the actual implementation and ratified authority and make your own changes.

Authority precedence remains `spec/source-request.md` > `spec/product.md` >
`spec/architecture.md` > `spec/threat-model.md` > `spec/verification.md` >
`spec/cli.md`. Kindex is context only, never authority.

## Required outcome and lane boundary

Finish the complete running P-1 through P-10 system and Architecture sections
1–12. This remains the proof implementation, not a scaffold: heterogeneous
checkpointed ingestion, three physically distinct stores/capabilities,
classification and taint-safe multi-destination exact-byte approval, maintained
validity/conflict/supersession state, temporal discernment, real signed authority
escalation, set-conditional VOI projection, digest-bound Company references,
actual Codex and Claude hooks, and full eleven-arm experiment/evidence accounting.

You own implementation, packaging, migrations, operational docs, hook installers,
and implementation-only diagnostics. Do not open, list, search, infer, create, or
modify `tests/`, another lane, or Validator material. Do not modify `spec/**`,
`evidence/**`, or ratification receipts. Do not contact another author or use
shared Kindex/coordination state. Run only implementation-owned syntax, build,
lint, and direct smoke checks; never run the judging suite or declare a verdict.

Finish your self-audit against every frozen obligation, inspect your own diff and
status, end the repository-local Kindex tag, commit all implementation work to the
bound branch, and leave the tree clean.

If frozen authority is genuinely contradictory and forces a semantic guess, stop
with exactly `FACTORY_QUESTION: <one concrete question>`. Otherwise end with
`FACTORY_STATUS: DONE <full commit SHA>` or
`FACTORY_STATUS: BLOCKED <reason>`. A lane status is not a verdict.
