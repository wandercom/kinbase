# Auxiliary-corpus rights grant — TEMPLATE, UNSIGNED

This file is a template. It is deliberately unsigned and carries no authority.
Committing a signed instance as `GRANT.md` is what closes Detector Reviewer
finding 21.

The signing party must be a named human with authority over the bytes listed
under `candidates` in `pool.json`. Replace every angle-bracketed field and sign.

---

I, <FULL LEGAL NAME>, of <AFFILIATION OR "individual">, state that:

1. I hold, or am authorised to grant rights in, the five documents under
   `tests/fixtures/auxiliary/sources/` whose SHA-256 digests are listed in
   `tests/fixtures/auxiliary/pool.json` under `candidates`.

2. The basis of that authority is: <EMPLOYMENT / SOLE AUTHORSHIP / ASSIGNMENT /
   OTHER — state it exactly>.

3. I dedicate those exact bytes to the public domain under CC0 1.0 Universal,
   the full text of which is at https://creativecommons.org/publicdomain/zero/1.0/legalcode
   and whose SHA-256 digest, as retrieved and committed alongside this grant,
   is <CC0_LEGALCODE_SHA256>.

4. That dedication permits, without further permission: local evaluation, and
   transmission to the model provider named in the experiment manifest.

5. I make this statement over the pool manifest digest <POOL_DIGEST>, and
   understand that changing any candidate byte invalidates it.

Signed: <SIGNATURE>
Name:   <FULL LEGAL NAME>
Date:   <ISO-8601 DATE>
